// Spectator camera, per-driver POV/action telemetry, and full-run learning-progress rendering.
function updateMainCamera(){const car=drivers[sim.selected],f=new THREE.Vector3(Math.cos(car.heading),0,Math.sin(car.heading)),roadY=surfaceHeightForCar(car);if(sim.cameraMode==='chase'){const desired=new THREE.Vector3(car.x-f.x*7,roadY+3.7,car.z-f.z*7);mainCamera.position.lerp(desired,.10);mainCamera.lookAt(car.x+f.x*5,roadY+.65,car.z+f.z*5)}else{mainCamera.position.set(car.x+f.x*.25,roadY+1.25,car.z+f.z*.25);mainCamera.lookAt(car.x+f.x*14,roadY+.65,car.z+f.z*14)}}
function resize(){const w=container.clientWidth,h=container.clientHeight;renderer.setSize(w,h,false);mainCamera.aspect=w/h;mainCamera.updateProjectionMatrix()}window.addEventListener('resize',resize);resize();
function log(text){const el=$('log'),line=document.createElement('div');line.textContent=text;el.prepend(line);while(el.children.length>28)el.lastChild.remove()}
function formatDuration(seconds){seconds=Math.max(0,Math.floor(seconds||0));const h=Math.floor(seconds/3600),m=Math.floor(seconds%3600/60),s=seconds%60;return h?`${h}h ${String(m).padStart(2,'0')}m`:m?`${m}m ${String(s).padStart(2,'0')}s`:`${s}s`}
function pctProgress(car){return mod(car.trackIndex-finishIndex,TRACK_N)/TRACK_N}
function actionName(car){return{steer:car.actionSteer<-.75?'HARD LEFT':car.actionSteer<-.1?'LEFT':car.actionSteer>.75?'HARD RIGHT':car.actionSteer>.1?'RIGHT':'STRAIGHT',throttle:car.actionThrottle>0?'THROTTLE':car.actionThrottle<0?'BRAKE':'COAST'}}
function updateDriversUI(){
  const wrap=$('drivers');wrap.innerHTML='';drivers.forEach((c,i)=>{const names=actionName(c),d=document.createElement('div');d.className='driver'+(i===sim.selected?' active':'');const status=sim.mode==='race'?(c.finishPlace!=null?`P${c.finishPlace} · ${c.finishTime.toFixed(1)}s`:c.raceStatus==='dnf'?'DNF':`lap ${Math.min(c.lap+1,sim.raceLaps)}/${sim.raceLaps}`):`${c.lap} lap${c.lap===1?'':'s'}`;d.innerHTML=`<div class="driver-head"><strong>Driver ${i+1}</strong><span>${status}</span></div><canvas class="driver-pov" width="32" height="20" aria-label="Driver ${i+1} neural camera input"></canvas><div class="driver-actions"><div><small>Steering</small><b>${names.steer}</b></div><div><small>Throttle</small><b>${names.throttle}</b></div><div><small>Confidence</small><b>${c.lastProb?`${Math.round(c.lastProb*100)}%`:'—'}</b></div><div><small>Gear</small><b>G${c.gear}</b></div></div><div class="mini"><span>speed <b>${c.speed.toFixed(1)}</b></span><span>run <b>${c.episodePeakProgress.toFixed(0)}m</b></span><span>ep reward <b>${c.episodeReward.toFixed(1)}</b></span><span>damage <b>${c.damage.toFixed(0)}%</b></span><span>hits <b>${c.collisions}</b></span><span>passes <b>${c.overtakes}</b></span></div><div class="damage"><div style="width:${c.damage}%"></div></div>`;d.addEventListener('click',()=>{sim.selected=i;updateUI()});wrap.appendChild(d);drawPreview(d.querySelector('.driver-pov'),c.latestRGBA)})
}
function liveMetrics(){const live=drivers.map(c=>c.episodePeakProgress),sum=sim.batchEpisodeDistance+live.reduce((a,b)=>a+b,0),count=sim.batchEpisodes+DRIVER_COUNT,batchBest=Math.max(sim.batchBestRunDistance,...live);return{update:sim.update+sim.experience/BATCH_TARGET,rewardPerExp:sim.batchReward/Math.max(1,sim.experience),forwardPerExp:sim.batchForwardMeters/Math.max(1,sim.experience),offRoadPct:sim.batchOffRoadSeconds/Math.max(.001,sim.batchDriverSeconds)*100,avgRunDistance:sum/Math.max(1,count),batchBestRunDistance:batchBest,bestRunDistance:Math.max(sim.bestRunDistance,batchBest),resets:sim.batchResets,laps:sim.batchLaps,collisions:sim.collisions,totalExperience:sim.totalExperience,wallSeconds:sim.trainingWallSeconds,simSeconds:sim.trainingSimSeconds,steer:sim.batchSteerCounts,throttle:sim.batchThrottleCounts,live:true}}
function currentMetrics(){if(sim.lastMetrics&&Number.isFinite(sim.lastMetrics.avgRunDistance))return sim.lastMetrics;return liveMetrics()}
function chartPoints(){
  const completed=sim.history.filter(x=>Number.isFinite(x.avgRunDistance)),points=[{update:0,avgRunDistance:0,batchBestRunDistance:0,bestRunDistance:0},...completed];if(sim.mode==='learn'&&sim.experience>0)points.push(liveMetrics());
  if(points.length<=500)return points;const sampled=[points[0]],stride=Math.ceil((points.length-2)/498);for(let i=1;i<points.length-1;i+=stride)sampled.push(points[i]);sampled.push(points.at(-1));return sampled;
}
function drawProgressChart(){
  const canvas=$('learningChart');if(!canvas)return;const ctx=canvas.getContext('2d'),w=canvas.width,h=canvas.height,padL=42,padR=12,padT=16,padB=24;ctx.clearRect(0,0,w,h);ctx.fillStyle='#08131d';ctx.fillRect(0,0,w,h);
  const points=chartPoints();if(points.length<2){ctx.fillStyle='#718493';ctx.font='11px system-ui';ctx.fillText('Run-distance history begins as soon as training moves.',14,h/2);return}
  const maxUpdate=Math.max(1,...points.map(m=>Number(m.update)||0)),maxY=Math.max(10,...points.flatMap(m=>[Number(m.avgRunDistance)||0,Number(m.batchBestRunDistance)||0]))*1.08,x=m=>padL+(Number(m.update)||0)/maxUpdate*(w-padL-padR),y=v=>padT+(1-v/maxY)*(h-padT-padB);
  ctx.strokeStyle='rgba(255,255,255,.10)';ctx.lineWidth=1;for(let k=0;k<=4;k++){const yy=padT+k/4*(h-padT-padB);ctx.beginPath();ctx.moveTo(padL,yy);ctx.lineTo(w-padR,yy);ctx.stroke();ctx.fillStyle='#718493';ctx.font='10px system-ui';ctx.fillText(`${Math.round(maxY*(1-k/4))}m`,4,yy+3)}
  const draw=(key,color)=>{ctx.strokeStyle=color;ctx.lineWidth=2;ctx.beginPath();points.forEach((m,i)=>{const v=Number(m[key])||0;i?ctx.lineTo(x(m),y(v)):ctx.moveTo(x(m),y(v))});ctx.stroke()};draw('avgRunDistance','#55dbea');draw('batchBestRunDistance','#ffd166');
  const last=points.at(-1);if(last?.live){ctx.fillStyle='#f4f7fb';ctx.beginPath();ctx.arc(x(last),y(last.avgRunDistance),3.5,0,Math.PI*2);ctx.fill()}
  ctx.fillStyle='#718493';ctx.font='10px system-ui';ctx.fillText('update 0',padL,h-6);const lastLabel=`${maxUpdate.toFixed(maxUpdate<10?1:0)}`;ctx.fillText(lastLabel,w-padR-ctx.measureText(lastLabel).width,h-6);ctx.fillStyle='#55dbea';ctx.fillText('avg run',padL+4,13);ctx.fillStyle='#ffd166';ctx.fillText('best run',padL+66,13);if(last?.live){ctx.fillStyle='#f4f7fb';ctx.fillText('• live batch',padL+132,13)}
}
function actionMixText(metrics){const steer=metrics.steer||[0,0,0,0,0],throttle=metrics.throttle||[0,0,0],steerTotal=steer.reduce((a,b)=>a+b,0)||1,driveTotal=throttle.reduce((a,b)=>a+b,0)||1,pct=(n,t)=>Math.round(n/t*100);return `Steer: L ${pct(steer[0]+steer[1],steerTotal)}% · straight ${pct(steer[2],steerTotal)}% · R ${pct(steer[3]+steer[4],steerTotal)}%  |  Drive: brake ${pct(throttle[0],driveTotal)}% · coast ${pct(throttle[1],driveTotal)}% · throttle ${pct(throttle[2],driveTotal)}%`}
function updateUI(){
  const headless=headlessActive();$('phaseText').textContent=sim.learning?'BACKPROP':sim.running?(sim.mode==='race'?'EVAL RACE':headless?'HEADLESS LEARN':'LEARNING'):'PAUSED';$('updateText').textContent=sim.update;$('experienceText').textContent=sim.mode==='race'?'frozen':`${sim.experience} / ${BATCH_TARGET}`;$('selectedText').textContent=`Driver ${sim.selected+1}`;$('trackText').textContent=TRACK_DEFS[activeTrackId]?.name||activeTrackId;$('actualSpeed').textContent=`${sim.achievedSpeed.toFixed(1)}×`;$('expLabel').textContent=sim.mode==='race'?'frozen':`${sim.experience} / ${BATCH_TARGET}`;$('expBar').style.width=sim.mode==='race'?'0%':`${Math.min(100,sim.experience/BATCH_TARGET*100)}%`;
  const metrics=currentMetrics(),liveBest=Math.max(0,...drivers.map(c=>c.episodePeakProgress)),bestEver=Math.max(Number(metrics.bestRunDistance)||0,sim.bestRunDistance,liveBest);$('rewardPerExp').textContent=sim.mode==='race'?'—':Number(metrics.rewardPerExp||0).toFixed(3);$('avgRunDistance').textContent=sim.mode==='race'?'—':`${Number(metrics.avgRunDistance||0).toFixed(0)}m`;$('batchBestRun').textContent=sim.mode==='race'?'—':`${Number(metrics.batchBestRunDistance||0).toFixed(0)}m`;$('bestRunDistance').textContent=`${bestEver.toFixed(0)}m`;$('offRoadPct').textContent=sim.mode==='race'?'—':`${Number(metrics.offRoadPct||0).toFixed(0)}%`;$('resetCount').textContent=sim.mode==='race'?'—':Number(metrics.resets||0);$('totalExperience').textContent=sim.totalExperience.toLocaleString();$('trainingTime').textContent=formatDuration(sim.trainingWallSeconds);$('simTrainingTime').textContent=formatDuration(sim.trainingSimSeconds);$('actionMix').textContent=actionMixText(metrics);
  $('runBtn').textContent=sim.mode==='race'?(sim.running?'Ⅱ Pause race':'▶ Resume race'):(sim.running?'Ⅱ Pause learning':'▶ Start learning');$('learnBtn').classList.toggle('primary',sim.mode==='learn');$('raceBtn').classList.toggle('primary',sim.mode==='race'&&!sim.raceFinished);$('headlessBtn').classList.toggle('primary',sim.headless);$('headlessBtn').textContent=sim.headless?'⚡ Headless display ON':'⚡ Headless display';if(sim.mode==='race'&&!sim.raceFinished)$('raceResult').innerHTML=`<strong>Race on ${TRACK_DEFS[activeTrackId].name}:</strong> ${sim.raceLaps} laps · ${sim.raceTime.toFixed(1)} s · ${raceResultsText()}`;
  if(!headless){updateDriversUI();drawProgressChart()}
}

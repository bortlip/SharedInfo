// Reproducible experiment comparison across saved brain training histories.
let lastExperimentComparisonSig='';
function completedBrainHistory(brain){return Array.isArray(brain?.training?.history)?brain.training.history.filter(m=>Number.isFinite(Number(m?.totalExperience))):[]}
function closestMetricAtExperience(brain,target){const history=completedBrainHistory(brain);if(!history.length)return null;let best=history[0],bestDelta=Math.abs(Number(best.totalExperience)-target);for(const metric of history){const delta=Math.abs(Number(metric.totalExperience)-target);if(delta<bestDelta){best=metric;bestDelta=delta}}return best}
function comparisonMetricForBrain(brain,mode,target){const history=completedBrainHistory(brain);if(!history.length)return null;return mode==='latest'?history.at(-1):closestMetricAtExperience(brain,target)}
function comparisonContext(){const brains=labSession?.brains||[],trained=brains.filter(brain=>completedBrainHistory(brain).length),mode=$('compareBudgetMode')?.value==='latest'?'latest':'common',target=mode==='common'&&trained.length?Math.min(...trained.map(brain=>Number(completedBrainHistory(brain).at(-1).totalExperience)||0)):null;return{brains,trained,mode,target}}
function metricTrackLayoutVersion(brain,metric){return Math.max(1,Math.trunc(Number(metric?.trackLayoutVersion??brain?.experiment?.trackLayoutVersion??1)||1))}
function metricReplayableFromBrainStart(brain,metric){return brain?.experiment?.seededFromStart===true&&Math.max(1,Math.trunc(Number(brain?.experiment?.trackLayoutVersion)||1))===metricTrackLayoutVersion(brain,metric)}
function comparisonConditionSummary(brains,mode,target){
  const entries=brains.map(brain=>({brain,metric:comparisonMetricForBrain(brain,mode,target)})).filter(x=>x.metric);
  if(entries.length<2)return'Add training history to at least two brains to compare matched conditions.';
  const seeds=[...new Set(entries.map(x=>brainExperimentSeed(x.brain)))],tracks=[...new Set(entries.map(x=>x.metric.trackId||'unknown'))],layouts=[...new Set(entries.map(x=>metricTrackLayoutVersion(x.brain,x.metric)))],replayable=entries.every(x=>metricReplayableFromBrainStart(x.brain,x.metric));
  if(seeds.length===1&&tracks.length===1&&layouts.length===1&&replayable)return`Matched seed ${seeds[0]} · ${TRACK_DEFS[tracks[0]]?.name||tracks[0]} · track layout v${layouts[0]} ✓`;
  const issues=[];if(seeds.length!==1)issues.push(`${seeds.length} seeds`);if(tracks.length!==1)issues.push(`${tracks.length} tracks`);if(layouts.length!==1)issues.push(`${layouts.length} track-layout revisions`);if(!replayable)issues.push('one or more runs are not replayable from their displayed starting provenance');return`Not fully matched: ${issues.join(' · ')}.`;
}
function formatComparisonNumber(value,digits=0,suffix=''){const n=Number(value);return Number.isFinite(n)?`${n.toFixed(digits)}${suffix}`:'—'}
function renderExperimentComparison(){
  const wrap=$('experimentComparison'),hint=$('comparisonHint');if(!wrap||!labSession)return;
  const{brains,trained,mode,target}=comparisonContext(),sig=[mode,target,...brains.map(brain=>{const history=completedBrainHistory(brain),last=history.at(-1);return`${brain.id}:${brain.name}:${brain.config?.visionId}:${brain.config?.networkId}:${brainExperimentSeed(brain)}:${brain.experiment?.seededFromStart}:${brain.experiment?.trackLayoutVersion||1}:${history.length}:${last?.totalExperience||0}:${last?.update||0}:${last?.trackId||''}:${last?.trackLayoutVersion||1}:${last?.bestRunDistance||0}:${JSON.stringify(last?.ppo||brain.training?.ppo||{})}`})].join('|');
  if(sig===lastExperimentComparisonSig)return;lastExperimentComparisonSig=sig;
  if(hint){const budgetText=mode==='latest'?'Latest completed PPO update for each brain':trained.length?`Closest completed update to ${Number(target).toLocaleString()} experiences for each trained brain`:'Matched budget becomes available after training';hint.textContent=`${budgetText}. ${comparisonConditionSummary(brains,mode,target)}`}
  if(!brains.length){wrap.innerHTML='<div class="comparison-empty">No brains in this session.</div>';return}
  const rows=brains.map(brain=>{const metric=comparisonMetricForBrain(brain,mode,target),seed=brainExperimentSeed(brain),replayable=metric?metricReplayableFromBrainStart(brain,metric):brain.experiment?.seededFromStart===true,seedLabel=replayable?String(seed):`${seed}*`,layoutVersion=metricTrackLayoutVersion(brain,metric),ppo=normalizedPpoSettings(metric?.ppo||brain.training?.ppo||PPO_DEFAULTS),track=metric?.trackId?TRACK_DEFS[metric.trackId]?.name||metric.trackId:'—';return`<tr><td><strong>${escapeHtml(brain.name)}</strong><small>${escapeHtml(brainConfigLabel(brain.config))}</small></td><td>${seedLabel}</td><td>v${layoutVersion}</td><td>${metric?Number(metric.totalExperience).toLocaleString():'—'}</td><td>${metric?Number(metric.update).toLocaleString():'—'}</td><td>${escapeHtml(track)}</td><td>${metric?formatComparisonNumber(metric.avgRunDistance,0,'m'):'—'}</td><td>${metric?formatComparisonNumber(metric.bestRunDistance,0,'m'):'—'}</td><td>${metric?formatComparisonNumber(metric.rewardPerExp,3):'—'}</td><td>${metric?formatComparisonNumber(metric.offRoadPct,0,'%'):'—'}</td><td>${metric?formatMs(metric.avgPpoMs):'—'}</td><td><small>${escapeHtml(`${ppo.batchTarget}/${ppo.epochs}× · ${ppo.learningRate} · clip ${ppo.clip}`)}</small></td></tr>`}).join('');
  wrap.innerHTML=`<div class="comparison-table-wrap"><table class="comparison-table"><thead><tr><th>Brain / architecture</th><th>Seed</th><th>Layout</th><th>Experience</th><th>Update</th><th>Track</th><th>Avg run</th><th>Best</th><th>R/exp</th><th>Off-road</th><th>Avg PPO</th><th>PPO setup</th></tr></thead><tbody>${rows}</tbody></table></div><div class="comparison-footnote">* run is not replayable from the displayed seed under the selected metric's track-layout revision (for example, it predates seeded runs or continued across a geometry revision).</div>`;
}
